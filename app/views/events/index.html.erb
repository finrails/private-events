<section class="container">
  <div class="row">
    <article class="col">
      <% @events.each do |event| %>
        <div class="card my-2">
          <div class="card-body">
            <b class="card-title"><%= event.creator.email %></b>
            <p class="card-text"><%= event.date %></p>
          </div>
        </div>
      <% end %>
    </article>
  </div>
</section>
